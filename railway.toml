[build]
builder = "dockerfile"
dockerfilePath = "Dockerfile"

[deploy]
startCommand = "sh -c 'python manage.py migrate && python manage.py collectstatic --noinput && gunicorn projectDjango.wsgi:application --bind 0.0.0.0:$PORT'"