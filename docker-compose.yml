services:
  web:
    build: .
    restart: always
    command: >
      sh -c "python manage.py migrate &&
             gunicorn projectDjango.wsgi:application --bind 0.0.0.0:8080"
    volumes:
      - sqlite_data:/app/data
      - media_volume:/app/media
    env_file: .env

  nginx:
    image: nginx:alpine
    restart: always
    ports:
      - "80:80"
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/conf.d/default.conf
      - media_volume:/app/media
    depends_on:
      - web

volumes:
  media_volume:
  sqlite_data: